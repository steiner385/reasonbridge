[features]
hooks = true

[[hooks.PreToolUse]]
matcher = "^Bash$"

[[hooks.PreToolUse.hooks]]
type = "command"
command = '''bash -c 'root=$(git rev-parse --show-toplevel 2>/dev/null); [ -n "$root" ] && bash "$root/.claude/hooks/block-no-verify.sh"' '''
