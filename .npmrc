# pnpm workspace configuration
# Enable hoisting to make root devDependencies accessible to all workspaces
public-hoist-pattern[]=*allure*
public-hoist-pattern[]=*vitest*

# Shared lockfile across all workspaces
shared-workspace-lockfile=true
